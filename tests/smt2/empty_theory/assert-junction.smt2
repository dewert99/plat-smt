(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(declare-const d Bool)
(push)
(assert (= a (and a b false)))
(assert (= b (and b c (not c))))
(assert (= c (and c d)))
(assert (= d (and)))
(check-sat)
(get-model)
(pop)

(push)
(assert (= a (or a b false)))
(assert (= b (not (or b c (not c)))))
(assert (= c (or c d)))
(assert (= d (and c d)))
(check-sat)
(get-model)
(pop)

(push)
(assert (= a (not (or a b c d))))
(push)
(assert a)
(check-sat)
(pop)
(assert (and (not b) (not c)))
(check-sat)
(get-model)
(assert (not d))
(check-sat)
(pop)

(push)
(assert (= (not a) (and a b c d)))
(push)
(assert (not a))
(check-sat)
(pop)
(assert (and b c))
(check-sat)
(get-model)
(assert d)
(check-sat)
(pop)

