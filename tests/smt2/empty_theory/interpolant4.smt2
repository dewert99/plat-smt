(declare-const p Bool)
(declare-const q Bool)
(declare-const r Bool)
(declare-const s Bool)
(define-const a1 Bool (=> p r))
(define-const a2 Bool (=> (not p) r))
(define-const b1s Bool (=> q (not r)))
(define-const b2 Bool (=> (not q) (not r)))
(check-sat-assuming (a1 a2 b1s b2))
(get-interpolants (and a1 a2) (and b1s b2))
(define-const b1w Bool (=> q s (not r)))
(check-sat-assuming (a1 a2 b1w b2))
(get-model)
(define-const a3 Bool (=> r s))
(check-sat-assuming (a1 a2 a3 b1w b2))
(get-interpolants (and a1 a2 a3) (and b1w b2))