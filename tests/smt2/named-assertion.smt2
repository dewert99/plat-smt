(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(assert (! a :named an))
(assert (! b :named bn))
(assert (! c :named cn))
(check-sat)
(get-model)
(check-sat-assuming ((not (and an bn))))
(get-unsat-core)