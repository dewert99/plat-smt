(declare-const a Real)
(declare-const b Real)
(declare-const c Real)
(declare-const a_bool Bool)
(declare-const b_bool Bool)
(declare-const c_bool Bool)
(define-const d Real (+ a b c))
(assert (! (>= a 0) :named al))
(assert (! (<= a 1) :named au))
(assert (=> a_bool (>= a 1)))
(assert (=> (not a_bool) (<= a 0)))
(assert (! (>= b 0) :named bl))
(assert (! (<= b 1) :named bu))
(assert (=> b_bool (>= b 1)))
(assert (=> (not b_bool) (<= b 0)))
(assert (! (>= c 0) :named cl))
(assert (! (<= c 1) :named cu))
(assert (=> c_bool (>= c 1)))
(assert (=> (not c_bool) (<= c 0)))
(check-sat-assuming ((xor a_bool b_bool c_bool) (>= d 2)))
(get-model)
(check-sat-assuming ((xor a_bool b_bool c_bool) (<= d 2)))
(get-model)
(check-sat-assuming ((or a_bool b_bool c_bool) (<= d 0)))
(get-unsat-assumptions)
(get-unsat-core)
(check-sat-assuming ((xor a_bool b_bool c_bool) (or (= d 2) (= d 0))))
(get-unsat-assumptions)
(get-unsat-core)