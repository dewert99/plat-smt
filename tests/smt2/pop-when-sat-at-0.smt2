(declare-const a Bool)
(declare-const b Bool)
(declare-fun f (Bool) Bool)
(push)
(assert (f b))
(assert (or a b))
(assert (or a (not b)))
(assert (or (not a) b))
(assert (or (not a) (not b)))
(push)
(check-sat)
(pop)
(check-sat)
(pop)
(assert (not (f b)))
(check-sat)