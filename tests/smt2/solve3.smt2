(declare-sort S1)
(declare-const a S1)
(declare-fun b () S1)
(declare-const x Bool)
(assert x)
(assert (= a b))
(check-sat)
(get-value (a b x))
(get-model)
(check-sat-assuming ((not (and x (= (a) (b))))))
(get-unsat-core)