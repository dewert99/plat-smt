(declare-sort D)
(declare-const a D)
(declare-const b D)
(declare-const c D)
(declare-const d D)
(declare-const p Bool)
(assert (or (distinct a d (ite p d c)) (not (distinct a d (ite p c b)))))
(assert (= p (distinct c a d)))
(check-sat)
(check-sat-assuming ((distinct d a c)))
(check-sat-assuming ((distinct a b d)))