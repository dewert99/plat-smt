(declare-sort I)
(declare-const r I)
(declare-const i Bool)
(declare-const t I)
(declare-const e I)
(push)
(assert (= r (ite i t e)))
(check-sat)
(get-model)
(pop)
(assert (not (= r (ite i t e))))
(check-sat)
(get-model)
