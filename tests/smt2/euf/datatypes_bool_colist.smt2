(declare-codatatypes ((BoolList 0)) (((empty) (cons (first Bool) (rest BoolList)))))
(declare-codatatypes ((BoolListPair 0)) (((bool-list-pair (bl0 BoolList) (bl1 BoolList)))))
(declare-const a BoolList)
(define-const b BoolList (cons false a))
(check-sat-assuming ((= b empty)))
(check-sat-assuming ((= b (cons true a))))
(check-sat-assuming ((= b (cons false b))))
(check-sat-assuming ((= b (cons false b)) (= a empty)))
(check-sat-assuming ((= b (cons false b)) (not (= a (cons (first a) (rest a))))))
(check-sat-assuming ((match a (((empty) false) ((cons f r) (and (= r b) f))))))
(check-sat-assuming ((and (not (first (rest b))) (match a (((empty) false) ((cons f r) (and (= r b) f)))))))