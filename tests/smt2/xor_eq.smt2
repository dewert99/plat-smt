(declare-const a Bool)
(declare-const b Bool)
(declare-const c Bool)
(push)
(assert (= a (xor b c)))
(check-sat-assuming (b c))
(get-model)
(pop)
(push)
(assert (= a (xor b true)))
(assert (= c (not (xor false a))))
(check-sat-assuming (b))
(get-model)
(pop)
(assert (xor a false))
(assert (not (xor true b)))
(assert (= c (xor true false)))
(check-sat)
(get-model)
(assert (xor true false))
(check-sat)
(assert (xor false false))
(check-sat)