(declare-sort I)
(declare-const a I)
(declare-const b I)
(declare-const c I)
(declare-const d I)
(declare-const e I)
(declare-const f I)
(assert (! (= a b) :named ab))
(assert (! (= b c) :named bc))
(assert (! (= c d) :named cd))
(assert (! (= d e) :named de))
(assert (! (= e f) :named ef))
(assert (! (not (= a f)) :named naf))
(check-sat)
(get-interpolants (and ab bc cd) (and de ef naf))
(get-interpolants (and naf ab ef) (and bc cd de))
(get-interpolants (and ab bc de) (and cd ef naf))