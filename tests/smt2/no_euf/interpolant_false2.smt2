(declare-const p Bool)
(define-const np Bool (not p))
(check-sat-assuming (p np))
(get-interpolants (and p) (and np))
(get-interpolants (and np) (and p))
(get-interpolants (and p np) (and))
(get-interpolants (and) (and p np))
(assert np)
(check-sat-assuming (p))
(get-unsat-core)
(get-interpolants (and p) (and))
(get-interpolants (and) (and p))